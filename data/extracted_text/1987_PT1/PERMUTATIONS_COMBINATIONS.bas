10  TEXT : HOME : INPUT "ENTER N,R (1<R<=N): ";N,R: PRINT :A$ = "1": FOR Q = N TO N - R + 1 STEP  - 1: FOR J =  LEN(A$) TO 1 STEP  - 1:P =  VAL( MID$ (A$,J,1)) * Q + C%:C% = P / 10:U = P - 10 * C%:P$ =  STR$(U) + P$: NEXT :S$ =  STR$(C%):P$ =  MID$ (S$,1,(C% <  > 0) *  LEN(S$)) + P$:C% = 0:A$ = P$:P$ = "": NEXT : PRINT "P(N,R) = ";A$
20  FOR K = 2 TO R:L =  LEN( STR$(K)):H = L:X$ = "":M$ =  LEFT$(A$,L):T =  VAL(M$) / K < 1:M$ =  LEFT$(A$,L + T):H = H + T: FOR Y = 1 TO 999:Q% =  VAL(M$) / K:X$ = X$ +  STR$(Q%):M = K * Q%:D =  VAL(M$) - M:H = H + 1:M$ =  STR$(D) +  MID$ (A$,H,1):Y = Y + 999 * (H >  LEN(A$)): NEXT :A$ = X$:H = 0: NEXT : PRINT : PRINT "C(N,R) = ";A$