1  HOME : PRINT " SCORE ->":Q = 1:SX = 1: FOR A = 1 TO 10:I =  INT( RND(1) * 2 + 1): FOR X = 2 TO 38 STEP I: VTAB 4: HTAB X: PRINT "  *":PX =  INT( PDL(0) / 7 + 1): VTAB 20: HTAB Q: PRINT "   ";:Q = PX: HTAB PX: PRINT "-+-": IF  PEEK( - 16287) > 127 THEN  VTAB SY +  NOT SY: HTAB SX +  NOT SX: PRINT " ":SX = PX + 1:SY = 20
2 SY = SY - (SY <  > 0) * 2: HTAB SX: VTAB SY +  NOT SY: PRINT  CHR$((SY <  > 0) * 33): HTAB SX: VTAB SY + 2: PRINT " ":B = (SY = 4 AND  ABS(SX - X - 2) < 2): PRINT  CHR$(6 + B):S = S + B: VTAB 1: HTAB 12: PRINT S: NEXT : VTAB 4: HTAB 38: PRINT "   ": NEXT : CALL  - 958: PRINT "PLAY AGAIN?";: GET A$: IF A$ = "Y" THEN  RUN 